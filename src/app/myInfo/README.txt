If you downloaded this proyect from github, you must create
a file in this directory called myData.ts with the following
format:

export const weatherKey:string = 'your weatherapi key'
export const locationKey:string = 'your maptiler api key'
